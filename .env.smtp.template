# SMTP Configuration (Gmail SMTP Relay)
# Add these to your .env file for SMTP testing

# Gmail SMTP credentials
SMTP_USER=steve@garayinvestments.com
SMTP_PASSWORD=your_16_char_app_password_here

# Test recipient email
TEST_EMAIL=steve@houserenovatorsllc.com

# SMTP settings (default to Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# From address (Gmail alias)
SMTP_FROM=noreply@houserenovatorsllc.com
